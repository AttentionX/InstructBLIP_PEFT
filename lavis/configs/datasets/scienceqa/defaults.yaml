datasets:
  scienceqa:
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          storage: scienceqa/scienceqa_train.json
        val:
          storage: scienceqa/scienceqa_val.json
        test:
          storage: scienceqa/scienceqa_test.json
      images:
        storage: .
